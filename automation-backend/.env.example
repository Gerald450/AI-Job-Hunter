# Chrome DevTools Protocol endpoint (Chrome must be started with --remote-debugging-port)
CDP_URL=http://127.0.0.1:9222

# HTTP port for the automation API (extension calls this)
PORT=8090

# Directory for failure screenshots + HTML snapshots
ARTIFACTS_DIR=./artifacts

# Default retry budget for helper actions
DEFAULT_RETRIES=3
